

var count = $("<%= '#'+@del_model.id.to_s %>").parent().children().length;

if(count > 1){
	$("<%= '#'+@del_model.id.to_s %>").hide();
}else{
	$("<%= '#'+@del_model.id.to_s %>").parents(".until_here").hide();
}