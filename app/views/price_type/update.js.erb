$("#update_price_type").parents(".floating_box").hide();
<% toRender = render "pt_row.html", :pt => @up_pt %>
$("#<%= @up_pt.id %>").remove();
$("#price_type_index table").append("<%= escape_javascript toRender %>");