<h2>Iniciar Sesión</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="sign_field"><%= f.label :email, "Correo" %><br />
  <%= f.email_field :email %></div>

  <div class="sign_field"><%= f.label :password, "Contraseña: " %><br />
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
    <div class="sign_field"><%= f.check_box :remember_me %> <%= f.label :remember_me, "Recordar usuario" %></div>
  <% end -%>

  <div class="sign_field"><%= f.submit "Iniciar Sesión", :id => "submit"%></div>
<% end %>